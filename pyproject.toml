[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dotdotzip"
version = "0.2.2"
description = "A utility for generating \"zip slip\" payloads for testing zip file extraction vulnerabilities."

[project.scripts]
dotdotzip = "dotdotzip:main"